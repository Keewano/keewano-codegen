/**
 * Arguments of `removeEvent`.
 *
 * name - the event to take out of the set.
 * input - the definitions file.
 */
interface RemoveEventArgs {
  name: string;
  input: string;
}

export type { RemoveEventArgs };
